============
Installation
============

At the command line::

    $ easy_install django-publica-meta

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-meta
    $ pip install django-publica-meta
